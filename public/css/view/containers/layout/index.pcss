.layout {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 100vh;
}

.layout__header {
    box-shadow: 0 2px rgba(0, 0, 0, 0.2);
}

.layout__footer {
    margin: 50px auto 0;
    width: 100%;
    overflow: hidden;
    background: #3c3c3c;
}